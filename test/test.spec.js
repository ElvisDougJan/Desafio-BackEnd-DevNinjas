const assert = require('assert')
describe('Array', () => {
  describe('#indexOf()', () => {
    it('should return -1 when the value is not present', () => {
      assert.equal([1, 2, 3].indexOf(4), -1)
    })
  })
})

describe('Teste', () => {
  describe('Deve fazer algo', () => {
    it('SEI LA', () => {
      const resultado = 1 + 1
      assert.strict.equal(resultado, 2)
    })
  })
})
